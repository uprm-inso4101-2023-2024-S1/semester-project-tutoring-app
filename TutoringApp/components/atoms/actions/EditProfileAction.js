import React from 'react';
import {Text} from 'react-native'

/**
 * Component for editing the current users profile.
 * 
 * An action could be a button or icon that enables the user to perform the requested action.
 * - Ex. Edit icon or button that redirects to the ser's edit profile screen
 * 
 * @returns {JSX.Element} EditProfileAction Component
 */
const EditProfileAction = () => {
    return (
        <Text>Edit Profile</Text>
    );
};

export default EditProfileAction;
