import React from 'react';
import {Text} from 'react-native'

/**
 * Component for sending a message to a user.
 * 
 * An action could be a button or icon that enables the user to perform the requested action.
 *  - Ex. Button that redirects to another page or opens a modal window
 * 
 * @returns {JSX.Element} SendMessageAction Component
 */
const SendMessageAction = () => {
    return (
        <Text>Message</Text>
    );
};

export default SendMessageAction;
